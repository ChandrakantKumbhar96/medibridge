server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: react-router owns client-side routes, nginx must not 404 them.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
